# Track-B ingestion container (VPS deployment): runs the APScheduler pollers.
FROM python:3.11-slim
WORKDIR /app
COPY pyproject.toml ./
COPY ingest ./ingest
COPY analytics ./analytics
COPY twin ./twin
COPY db ./db
RUN pip install --no-cache-dir -e .
CMD ["python", "-m", "ingest.scheduler"]
