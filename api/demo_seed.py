"""Offline demo dataset loader — the judging-day lifeline (agents.md §8).

TODO(P4.3): once schemas settle, populate stations/readings/hotspots/
source_factors with a frozen, realistic snapshot so the full dashboard demos
with the network cable unplugged.
"""


def main() -> None:
    print("demo-seed: TODO(P4.3) — frozen snapshot pending real accumulated data")


if __name__ == "__main__":
    main()
