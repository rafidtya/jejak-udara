"""Forecast backtesting: score persisted forecasts against later observations.

Honest bar (ASSUMPTIONS.md): beat naive persistence. Writes validation_runs
(kind='forecast_skill'). TODO(P3.6): wire once forecast_surfaces accumulate.
"""
from __future__ import annotations


def main() -> None:
    print("backtest: TODO(P3.6) — needs persisted forecasts + subsequent observations")


if __name__ == "__main__":
    main()
